# NinniTales Shorts pipeline — Python deps.
# System deps (install separately): ffmpeg, yt-dlp
#   macOS:  brew install ffmpeg yt-dlp
#   Ubuntu: sudo apt-get install -y ffmpeg && pipx install yt-dlp
requests>=2.31.0
certifi>=2024.2.2     # CA bundle for the image v1 REST call (urllib has no store on macOS)
openai>=1.30.0        # Azure OpenAI (GPT hook copy)
Pillow>=10.0.0        # caption compositing for generated hooks
