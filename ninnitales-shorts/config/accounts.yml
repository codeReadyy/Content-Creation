# The routing table — the single source of truth for WHO posts WHAT, WHEN.
# One block per (platform, account). The orchestrator iterates these.
#
# CONVENTION: ONE FORMAT PER ACCOUNT. A single feed should have one consistent
# content type (scraped footage OR anime OR carousel) — mixing formats in one
# channel confuses the audience and the algorithm. To compare formats, run the SAME
# niche on TWO accounts (each its own channel + creds_env), one format each, and let
# analyze.py rank them head-to-head ("ACCOUNT STANDINGS"). The orchestrator fills all
# of an account's slots with its single declared format; with one format there is no
# cross-format fallback, so a build failure skips the slot rather than posting an
# off-format clip.
#
# Fields:
#   id          unique account id (used in the ledger + account standings)
#   platform    youtube | instagram | tiktok
#   product     product key (forks swap this)
#   niche       config/niches/<niche>.yml — the content profile
#   creds_env   env-var SUFFIX for creds, e.g. NINNITALES -> YOUTUBE_*_NINNITALES
#   formats     the account's format — keep it to ONE (see convention above)
#   schedule_et publish times in US Eastern; build runs ~2h before the first slot
#   gate        true = require Telegram approval first; false = fully autonomous
#   enabled     false = ignored by the orchestrator (scaffolding for later phases)

accounts:
  # 2 slots, not 3: the Jul-2026 audit showed 3 near-identical Shorts/day from a small
  # channel meant ~2 of 3 got ZERO feed impressions (repetition sampling). Fewer,
  # distinct uploads → each one actually gets its feed test.
  - id: ninnitales_yt_main
    platform: youtube
    product: ninnitales
    niche: toddler_sleep
    creds_env: NINNITALES            # -> YOUTUBE_*_NINNITALES
    formats: [scraped_cta]           # proven ~50x winner; flip to [anime_cta] to switch
    schedule_et: ["08:00", "19:00"]
    gate: false
    enabled: true

  # A/B the anime format on its OWN channel (so neither feed mixes formats). Enable
  # once a second YouTube channel exists and its creds are added as YOUTUBE_*_NINNITALES_ANIME.
  - id: ninnitales_yt_anime
    platform: youtube
    product: ninnitales
    niche: toddler_sleep
    creds_env: NINNITALES_ANIME      # -> YOUTUBE_*_NINNITALES_ANIME
    formats: [anime_cta]
    schedule_et: ["08:00", "12:30", "19:00"]
    gate: false
    enabled: false

  # IG = value-first CAROUSELS (the save-magnet format for the parenting niche): unique
  # LLM copy + AI cover per post — fixes the duplicate-content demotion the recycled
  # reels + identical captions caused (Jul-2026 audit). Reels return later on a SECOND
  # IG account (pain-point hooks), ranked head-to-head by analyze.py account standings.
  # Two slots: 09:00 ET (proven US-midday) + 22:00 ET ≈ 7:30 AM IST (India-morning test).
  # On IG the cron time IS the post time (--now 1) — the crons in ninnitales-instagram.yml
  # actually drive timing; schedule_et just mirrors them for the record.
  - id: instagram_ninnitales
    platform: instagram
    product: ninnitales
    niche: toddler_sleep
    creds_env: NINNITALES
    formats: [carousel]
    schedule_et: ["09:00", "22:00"]   # 22:00 EDT == 02:00 UTC == 7:30 AM IST
    gate: false
    enabled: true

  # Pinterest — evergreen image pins (one per slot), recycled across boards. Pinterest has
  # no native scheduling, so the cron posts immediately (orchestrate.py --platform pinterest
  # --now N). 5 slots ≈ 5 pins/day; bump the workflow's --now / add slots to reach 5-10/day.
  # enabled:false until PINTEREST_* creds are connected via the connect-helper.
  - id: pinterest_ninnitales
    platform: pinterest
    product: ninnitales
    niche: toddler_sleep
    creds_env: NINNITALES            # -> PINTEREST_*_NINNITALES
    formats: [pin]
    schedule_et: ["08:00", "11:00", "14:00", "17:00", "20:00"]
    gate: false
    enabled: true
