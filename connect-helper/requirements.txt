flask>=3.0
requests>=2.31
cryptography>=42.0   # enables Flask's self-signed HTTPS (ssl_context="adhoc") for the IG redirect
ruamel.yaml>=0.18    # round-trip edit of accounts.yml (preserves comments)
